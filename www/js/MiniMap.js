function MiniMap(name, scene){
    // Minimap
    this.width = 0;
    this.height = 0;
    this.xPos = 0;
    this.yPos = 0;
    /*
    this.cam = new BABYLON.FreeCamera(name, new BABYLON.Vector3(0,100,0), scene);
    cam.layerMask = 1;
    cam.setTarget(new BABYLON.Vector3(0.1,0.1,0.1));
    cam.mode = BABYLON.Camera.ORTHOGRAPHIC_CAMERA;

    cam.orthoLeft = -this.size/2;
    cam.orthoRight = this.size/2;
    cam.orthoTop =  this.size/2;
    cam.orthoBottom = -this.size/2;
    cam.rotation.x = Math.PI/2;
    
    cam.viewport = new BABYLON.Viewport(
        this.xPos,
        this.yPos,
        this.width,
        this.height
    ); */ 
}